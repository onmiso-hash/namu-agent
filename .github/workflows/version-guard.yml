name: version-guard

on: [push, pull_request]

jobs:
  check-version-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 namu-plugin/test_version_drift.py
