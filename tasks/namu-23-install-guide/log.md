# log — namu-23-install-guide
(append만. context 꼬이면 이걸로 복원)

[시작] 2026-07-08 13:15:49 samsung · 작업 생성, 목적·완료조건 확정 — 사용자 확인 통과. 소재=로드맵 2단계 잔여(설치형 사용설명서, #20 deploy_design.md 토대). 합의안=① docs/install_guide.md 신설 ② GitHub 원격 marketplace 설치 실측(#18 미검증 경로, repo 밖 프로젝트 + /mcp·자동주입) ③ README.md 링크 연결. 범위 밖=영어판 README(로드맵 기존 분류 유지)·이종 엔진 어댑터(하드웨어 제약으로 계속 이월, 사용자 결정)
[분담] 2026-07-08 13:22:00 samsung · 분할안 4단계(A 초안 coder→B 원격 실측→C 반영 coder→D reviewer) 사용자 승인. 사전 확인=repo private(비인증 API 404)·marketplace.json이 namu-plugin/.claude-plugin/ 아래(루트 아님 → 원격 add 실패 가능성, 실측 포인트)·워커 native(비네임스페이스 namu-coder 존재). A: coder에 install_guide.md 초안 위임(소재 폐쇄, 원격 절차는 실측 대기 표기)
[결정] 2026-07-08 13:30:00 samsung · A 완료 — coder가 docs/install_guide.md 신설(8섹션+뺀 것 목록, 원격 절차는 4-2절 실측 대기 블록으로 격리, 미실측 명령어 0) + README.md 셋업 가이드에 링크 1줄. 오케스트레이터 검토=소재 폐쇄 준수 확인. 다음=B 원격 실측 계획 사용자 승인
[결정] 2026-07-08 13:38:51 samsung · B 실측(헤드리스분) 전판 PASS — ① 현 구조 add 실패 관측: clone은 성공(private repo HTTPS 자격증명 OK), 루트 .claude-plugin/marketplace.json 부재가 유일 원인(에러가 요구 경로 명시) ② 루트 봉투 신설·검증·푸시(e2fba2e) 후 add 성공. 🔑함정 신발견 2건: (a) 같은 이름 marketplace add는 기존 directory 등록을 조용히 대체(충돌 에러 없음 — 개발 기기 주의) (b) 원격 marketplace 캐시=repo 전체 clone(memory/ 포함)이나, 설치 실행본은 plugins/cache/<mp>/<plugin>/<버전>/ 플러그인 폴더만 복사 → REPO_ROOT에 memory/ 없어 분기 3 정상, 유령 경로 재발 없음(config.py 실독 확인) ③ user 스코프 원격 install 성공 — 0.1.5, gitCommitSha=e2fba2e(방금 푸시 커밋=GitHub발 물증), 기존 local 0.1.2 무사 ④ repo 밖 폴더 claude mcp list → plugin:namu:namu-memory 0.1.5 캐시 경로 ✔Connected. 남은 것=사용자 라이브(/mcp+자동주입)+원복
[막힘] 2026-07-08 13:52:00 samsung · B 라이브서 CC 자동주입 FAIL 발견 → 원인 확정: session_recall.py print가 Windows 파이프 cp949로 🌳📌 JSON 인코딩 실패(UnicodeEncodeError) → bare except가 무음 exit 0 → CC 2.1.0+ 무화면 사양과 겹쳐 완전 잠복. repo cwd 대조로 directory 설치도 동일 확인 = samsung CC 주입은 애초부터 전멸(hp는 리눅스 UTF-8이라 정상, 대칭 검증 사각지대). agy판 session_inject.py는 07-02에 같은 버그 수정(reconfigure utf-8)됐으나 CC판만 누락. 물증: python -X utf8 실행 시 완전한 JSON(namu-23 브리핑) / MCP·statusLine은 정상(스크린샷 image·image2). 사용자 승인=cp949 수정+0.1.6을 C단계 포함
[결정] 2026-07-08 15:05:00 samsung · C+D 완료 — coder: session_recall.py reconfigure(utf-8) 1줄+회귀테스트 2건(cp949 강제 subprocess, tmp 픽스처 격리)+0.1.6 범프 2곳+문서 반영(4-2 실측 대기 해소·함정 6~8). reviewer 검수 PASS(기준 6개 전부, 기존 실패 6건은 samsung 로케일/파일잠금 기존 이슈로 무관 확인 — 이월 후보). 오케스트레이터 스모크=uv 파이프서 JSON 정상. 커밋 b46e31d(fix)+6daad22(docs) 푸시. 이어서 plugin update 실측: `update namu`=not found 실패, `update namu@namu-marketplace`=0.1.5→0.1.6·sha 6daad22 반영 성공(전체 식별자 필수 — 문서 교정 커밋 50cbd5c, agy 원격 "미지원"→"미실측" 단정 완화 포함). 남은 것=사용자 라이브(0.1.6 자동주입)+원복+record
[완료] 2026-07-08 15:31:18 samsung · #23 종료 — 완료조건 전부 충족: ① docs/install_guide.md 신설(8섹션·함정 8종, 실측 대기 0) ② 원격 설치 실측 전판 PASS(루트 봉투→add→user 스코프 install sha물증→update 전체 식별자→새 세션 /mcp·자동주입 사용자 라이브, 스크린샷 3장 판독) ③ README 링크 1줄. 덤 2개=cp949 잠복 버그 발견·수정(0.1.6)+plugin update 명령 확정. 원복 완료(user 스코프 uninstall→directory 재등록→mcp ✔Connected 소스 직참조 확인). record 2건(01KX06QSYRRXWG6PBCVGXCS0JP cp949, 01KX06RG1QW9CEPSEXFGACZ22T 원격 marketplace, 둘 다 human). 이월 후보=samsung 기존 테스트 실패 6건(test_cache_stale 4·test_mcp_selfheal 2, 로케일/파일잠금)·영어판 README(공개 임박 시). 잔재=namu-install-test 폴더(사용자 세션 잠금, 터미널 닫은 뒤 삭제)
