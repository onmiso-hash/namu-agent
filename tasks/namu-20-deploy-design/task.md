# namu-20-deploy-design — 2단계 배포 형태 설계 (플러그인 설치형)
📅 생성 2026-07-06 [samsung] · 🔗 관련: namu-18-public-readme(현재형 문서화까지만) · namu-17(워커 배포 재검토 조건 발동)

## 목적
현재 NAMU는 "repo가 곧 NAMU_HOME"인 clone 기반으로만 살아왔다. 공개 배포(2단계)의
전제인 플러그인 설치형 — 임의 프로젝트에서 NAMU를 쓰는 형태 — 는 핵심 3요소가
전부 미결정. 설계와 실측으로 배포 형태를 확정해 설치형 사용설명서(별건)의 토대를 만든다.

## 완료조건
- [ ] ① 사용자 메모리 위치 설계·실측 — NAMU_HOME 분리 모드(repo 밖 데이터 폴더)에서 메모리 3층(learnings.yaml/tasks/db)이 실제 동작함을 라이브 확인 (한 번도 그 형태로 산 적 없음)
- [ ] ② 워커 정의 배포 방식 결정 — repo 밖 프로젝트에서 namu-coder/reviewer를 쓰는 경로(CC `.claude/agents` · agy `.agents/agents` 각각) 설계 + 근거 기록
- [ ] ③ agy repo 밖 실행 한계 실측 — repo 아닌 프로젝트에서 agy 플러그인·스킬·워커가 어디까지 동작하는지 확정 (막히는 지점 목록화)
- [ ] 결정 3건을 plan.md 결정 테이블에 기록 + 설계 문서화(docs/ 배치)

## 범위 밖
- 설치형 사용설명서·영어판 README — 본 설계·실측 확정 후 별건
- 이종 엔진 워커 어댑터 — 후속 이월(사용자 결정 2026-07-06)
- 3단계 공개 메모리 풀
