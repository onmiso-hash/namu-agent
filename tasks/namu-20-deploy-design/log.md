# log — namu-20-deploy-design
(append만. context 꼬이면 이걸로 복원)

[시작] 2026-07-06 14:22:26 samsung · 작업 생성, 목적·완료조건 확정 — 사용자 확인 통과. 소재=plan.md 로드맵 2단계 메모(미결정 3요소: ① NAMU_HOME 분리 모드 ② 워커 정의 배포 ③ agy repo 밖 한계). #19 종료 직후 연속 착수(사용자 순서 결정 2026-07-06)
[결정] 2026-07-06 14:26:58 samsung · 설계 게이트 통과(사용자 선택): ① 메모리 위치=A안(~/.namu 폴백 + NAMU_HOME env override, 미설정 시 자동 스캐폴딩 — 유령 경로 박멸+진입장벽 최소) ② 워커 배포=A안(플러그인 동봉 namu-plugin/agents/, 13번 보류분 자급자족 발동 — 양 엔진 로드는 실측 검증 조건부) ③ agy repo 밖 한계=실측 계획 확정(임시 워크스페이스서 스킬/MCP/워커 3단, 사용자 라이브+물증). 근거: config.py:16 폴백=REPO_ROOT가 플러그인 모드서 캐시 유령 경로(#13·#16 재발 위험), 워커 정의 수정 빈도 저하로 #17 핫리로드 논거 약화
[결정] 2026-07-06 14:43:57 samsung · 1차 구현(coder)+검수(reviewer) PASS — config.py NAMU_HOME 3분기 폴백(env → repo memory/ 실재 시 REPO_ROOT → ~/.namu), import 부수효과 0, plugin.json 2곳 0.1.2. 기존 테스트 task_resolve 5/5·session_context 6/6 통과, cache_stale 4건 실패는 수정 전 코드서도 재현되는 사전 존재 실패(Windows 핸들+DELETE LIMIT)로 판정. 병행 조사(CC 공식 문서): 플러그인 agents/ 자동 인식·플랫 .md·frontmatter 동일(hooks 등 3필드 불가), 단 이름이 "namu:namu-coder"로 네임스페이스됨 = namu_workers.yaml 호출명과 불일치(후속 설계 필요), 충돌 시 프로젝트 .claude/agents가 우선(개발 repo 안전). agy 플러그인 agents 형식은 여전히 미지 — install fail-fast로 실측 예정
[결정] 2026-07-06 14:47:45 samsung · agy 플러그인 agents 실측 2회(승인분): install#1 하위폴더 레이아웃(agents/<name>/agent.md)만 → "agents: 2 processed" = agy 봉투 인식 확정(#17 skipped서 진전), 설치처 ~/.gemini/config/plugins/namu/agents/ 확인. install#2 CC 플랫(.md) 추가 → "agents: 4 processed" = agy가 플랫까지 통째 스캔, 같은 이름 2벌 공존(플랫은 CC 도구명이라 agy invoke 시 깨질 정의) = 한 agents/ 폴더 양 엔진 공용 불가 판명. 해법 후보=agents/는 agy 전용, CC는 plugin.json "agents" 명시 경로 필드로 별도 폴더 지정 — 필드가 자동 스캔을 대체하는지 문서 후속 질의 중. 현 설치본은 충돌 상태(정리 install 1회 추가 필요, 사용자 승인 대기)
[결정] 2026-07-06 14:50:21 samsung · agents 레이아웃 확정(install#3+잔재청소 실측): agents/<name>/agent.md=agy 전용, cc-agents/<name>.md+plugin.json "agents" 필드=CC 전용(공식 문서 확답: 필드가 자동 스캔을 대체). install#3 직후 "4 processed"의 진범=agy plugin install이 삭제된 파일을 청소 안 하는 비파괴 병합(설치본에 #2 플랫 잔재 존속) — 수동 삭제 후 재install로 "agents: 2 processed"+설치본 하위폴더만 확인 = agy는 cc-agents/·plugin.json 필드 둘 다 무시 확정. ⚠️ 새 함정(배포 문서 기록 대상): agy 재설치는 파일 삭제를 반영 안 함, 잔재 수동 청소 필요. 남은 실측=CC(0.1.2 업데이트+재시작: cc-agents 로드·namu:namu-coder 네임스페이스·~/.namu 분리 모드)·agy repo 밖 워크스페이스. 남은 설계=워커 호출명 네임스페이스 처리(스킬/워커명단)
[분담] 2026-07-06 14:53:27 samsung · 사용자 승인으로 커밋 2건(b5a2c2d feat(plugin) 0.1.2 / 0247f66 docs(task)) 푸시 완료 + CC 플러그인 0.1.1→0.1.2 업데이트 성공(scope local, 적용은 재시작 시). 다음 세션(재시작 후) 실측 항목: ① /agents 또는 Agent 도구에서 namu:namu-coder·namu:namu-reviewer 노출 확인(cc-agents 로드+네임스페이스, agents/ 하위폴더 junk 미노출 확인) ② 임시 NAMU_HOME(분리 모드)로 recall/record가 ~/.namu 또는 지정 폴더에 쓰는지 ③ agy repo 밖 워크스페이스 실측(사용자 라이브+물증). 남은 설계=워커 호출명 폴백(스킬)·workers yaml 부재 처리 → 실측 결과 반영해 확정
[결정] 2026-07-06 15:00:31 samsung · CC 재시작 후 실측 ①② PASS. ① 새 세션 Agent 도구 목록에 namu:namu-coder·namu:namu-reviewer 노출(cc-agents 로드+네임스페이스 라이브 확정), agents/ 하위폴더(agy 전용) junk 미노출 = plugin.json "agents" 필드가 자동 스캔 대체함을 라이브 검증, 프로젝트 .claude/agents 무접두 2종과 공존(스킬 namu:namu-task도 동일 네임스페이스). ② 캐시 0.1.2 코드로 3분기 전수 실측: env override→지정 폴더에 memory/+db/ 스캐폴딩+record 성공(ULID 반환·yaml/db 생성), env 없음+캐시(memory/ 부재)→가짜홈/.namu 스캐폴딩+record 성공, env 없음+repo(memory/ 실재)→REPO_ROOT 산출. 부수 발견 2건: (a) CC 세션 프로세스 env에 repo .env의 NAMU_HOME·NAMU_MACHINE이 이미 실려 있어 자식 프로세스로 상속됨(실측 시 명시 제거 필요했음, dev 환경에선 정상 동작) (b) 분리 모드 신규 사용자는 NAMU_MACHINE 미설정 → machine="unknown" 기록 = 멀티 PC 식별 공백, 온보딩/스캐폴딩 설계 대상. 남은 실측=③ agy repo 밖(사용자 라이브). 남은 설계=워커 호출명 폴백·workers yaml 부재 처리·NAMU_MACHINE 온보딩
[결정] 2026-07-06 15:31:14 samsung · agy repo 밖 실측 ③ 완료(사용자 라이브, D:\Temp\namu-agy-ws, 물증 image~3.png+디스크). 3단 결과: (1) 스킬 PASS — /namu 자동완성에 namu-task 설명 노출, 단 호출명이 `/namu:SKILL` = agy는 폴더명(namu-task)이 아닌 파일명(SKILL.md)을 스킬명으로 사용(CC는 namu:namu-task → 엔진별 호출명 상이, 폴백 설계 데이터). (2) MCP FAIL — namu-memory 기동 실패: mcp_config.json의 상대경로 "namu-plugin/mcp_server.py"를 agy가 워크스페이스 CWD 기준으로 해석(에러 경로 D:\Temp\namu-agy-ws\namu-plugin\...). repo 안에서는 CWD=repo루트라 우연히 동작했던 것 = 이번 실측이 잡으려던 "repo 밖 한계" 실증. 설치본 경로 구조(플러그인 루트 직하)와도 불일치 → 수정 필수(agy의 플러그인 루트 변수 유무 조사 후 설계, CC는 ${CLAUDE_PLUGIN_ROOT}). (3) 워커 PASS — `Agent(namu-reviewer: Code Reviewer)` 실호출 성공, 판정 Pass 반환, hello.py 무변경(리뷰어 read-only 정합). 호출명=무접두 `namu-reviewer`(CC의 `namu:namu-coder`와 상이 → 워커 호출명도 엔진별 분기 확정). 부수 발견 2건: (a) PreInvocation 훅은 repo 밖에서 정상 작동 — ~/.namu/db/namu.db 15:26 자동 스캐폴딩 물증(3분기 폴백 agy 라이브 검증, memory/ 부재는 record 미발생과 정합). 훅이 되고 MCP가 죽는 이유=agy가 훅은 플러그인 설치 폴더 CWD로, MCP는 워크스페이스 CWD로 실행하기 때문(session_inject.py:50 주석과 이번 에러 경로가 교차 증명). (b) agy 서브에이전트가 린터 실행 시 사용자 응답 타임아웃 → 수동 검토 폴백 = agy 워커의 도구 승인 게이트 특성. 남은 설계: mcp_config 경로 수정 + 워커/스킬 호출명 폴백 + workers yaml 부재 + NAMU_MACHINE 온보딩
[결정] 2026-07-06 15:51:58 samsung · MCP 경로 실측+record 실측 완료(사용자 라이브, 물증 image4~5.png+디스크). (1) 경로 3후보 동시 실측(설치본 mcp_config에 t-var/t-tilde/t-abs 병렬 정의, 재시작 1회): t-abs(절대경로) ✓ Tools 3종 로드 / t-tilde(~) ✗ 리터럴 / t-var(${PLUGIN_ROOT}) ✗ 리터럴 그대로 자식에 전달 = agy는 ${...} 치환 메커니즘 자체가 없음(다른 변수 추가 실측 불요) → **agy 플러그인 MCP는 절대경로만 가능** 확정. 서버 코드 자체는 repo 밖 완벽 동작 — 결함은 경로뿐. (2) record 실측: t-abs/namu_record 호출 → ULID 01KWV32GZPFBZ1285KMFA8RZ8R 반환, 디스크 물증 정합 — ~/.namu/memory/learnings.yaml 15:50 생성·1건 append(내용 일치), db 15:50 갱신, machine: unknown(예측 그대로 = NAMU_MACHINE 온보딩 공백 재확인). 분리 모드 3분기 폴백 agy 라이브 전수 검증 완료. 부수 발견: 이 dev PC는 NAMU_MACHINE=samsung이 OS 사용자 환경변수로도 영구 등록 — 새 셸에도 실림 → 이 기기의 모든 분리 모드 실측은 세션 내 Remove-Item 필수(.env 상속과 별개 오염원). 남은 설계: mcp_config 절대경로 주입 방안(훅 self-healing vs 설치 스크립트/문서, 사용자 게이트 대기) + 워커/스킬 호출명 폴백 + workers yaml 부재 + NAMU_MACHINE 온보딩
[중단] 2026-07-06 16:01:03 samsung · 사용자 결정으로 오늘 세션 종료. 실측은 전수 완료(CC ①②·agy ③·MCP 경로·record), 설치본 mcp_config 절대경로 정상화 완료. 다음 세션 첫 안건=mcp_config 절대경로 주입 게이트 재안내(A안 훅 self-healing 추천 vs B안 설치 스크립트) → 사용자 선택 후 구현안 보고. 상태 파일·image~5.png 미커밋, 임시 산출물(D:\Temp\namu-agy-ws, ~/.namu) 잔존 — 정리 가능 상태
[결정] 2026-07-07 20:54:58 hp · CC 0.1.2 실측 PASS(hp, git pull 직후 세션): ① cc-agents 로드+네임스페이스 확정 — 세션 에이전트 목록에 `namu:namu-coder`/`namu:namu-reviewer` 등재, agy 전용 agents/ 하위폴더는 미로드(문서 주장 "agents 필드=자동 스캔 대체" 라이브 확인). ② ⚠️ 새 발견: directory 소스 마켓플레이스는 설치 캐시가 아니라 소스 라이브 참조 — installed_plugins.json은 0.1.1(캐시에 agents 없음)인데 0.1.2 소스의 agents가 로드됨 = dev 기기는 git pull+재시작만으로 반영, 재설치 불필요. 단 원격(github) 마켓플레이스 설치는 캐시 복사라 update 필요 — 배포 문서에 구분 기록 대상. ③ ~/.namu 분리 모드 실측 PASS(시뮬레이션): 플러그인을 repo 밖 가짜 캐시로 복사+가짜 HOME으로 3분기 폴백 확인(NAMU_HOME=→$HOME/.namu), record→recall 왕복(learnings.yaml append+FTS 히트)·tasks 층(find_active_task+세션 브리핑 생성)·db 스캐폴딩(mkdir parents) 전부 동작 = 완료조건 ① 충족. 남은 실측=CC 워커 라이브 호출(namu:namu-coder, 승인 게이트)·agy repo 밖 워크스페이스. 남은 설계=워커 호출명 네임스페이스 처리
[결정] 2026-07-07 21:28:22 hp · agy repo 밖 3단 실측 — 스킬 PASS·워커 설치 PASS·MCP FAIL(중대): ① 재설치는 uninstall→install이 정석(잔재 함정 해소, "agents: 2 processed" 확인). ② 빈 워크스페이스: 스킬(namu-task) 로드+본문 접근 정상, 훅 정상(NAMU_HOME 오버라이드 존중, db 스캐폴딩), MCP 도구는 "없음". ③ 핵심 발견 — agy 1.0.16 -p(비대화) 모드는 MCP 서버 기동을 시도만 하면 세션 전체가 멈춤(≥5분, 자체 print-timeout도 무시): 플러그인 동봉 mcp_config.json의 cwd 상대경로(namu-plugin/mcp_server.py)가 해석되는 폴더(repo·심링크·복사본)에서 전부 재현, 워크스페이스 .agents/mcp_config.json 절대경로 설정에서도 재현, --dangerously-skip-permissions로도 재현. 반증 대조군: 경로 미해석 폴더=즉답, 플러그인 disable=같은 폴더서 즉답. 서버 자체는 동일 조건 수동 부팅 1초 내 정상(initialize 핸드셰이크 OK) = 잘못은 agy 쪽 MCP 클라이언트/기동부. 부수 확인: repo 안 -p 타임아웃의 진범도 동일(상대경로가 repo서 해석돼 MCP 기동 시도), hp의 옛 .agents/mcp_config.json(절대경로+env 하드코딩)은 .bak으로 꺼져 있었음, agy 전역 mcp_config.json은 install "mcpServers: 1 processed"에도 빈 파일. 남은 실측=대화형 모드에서 MCP 동작 여부(사용자 라이브 1회, 원계획대로) → 결과에 따라 "agy 한계 목록"에 -p+MCP 불가 확정 기재
[결정] 2026-07-07 21:38:19 hp · agy 대화형 MCP 실측 PASS(사용자 라이브+스크린샷 물증) — 완료조건 ③ 충족: repo 밖 워크스페이스(agy-ws3)에서 대화형 agy가 namu-memory/namu_recall·namu_search 도구를 실제 호출, NAMU_HOME env 오버라이드 존중(빈 테스트 저장소라 결과 0건=정상), 도구 스키마 캐시는 ~/.gemini/antigravity-cli/mcp/namu-memory/*.json에서 로드 확인. agy 한계 목록 확정: ① -p(비대화) 모드+MCP 설정=세션 멈춤(유일한 실질 한계, NAMU엔 오케스트레이터=대화형이라 영향 제한적이나 배포 문서 명기) ② plugin install=비파괴 병합(해법: uninstall→install) ③ 한 agents/ 폴더 양 엔진 공용 불가(레이아웃 분리로 해소). 스킬·훅·워커정의·MCP(대화형) 전부 repo 밖 동작 확인. 남은 것=워커 호출명 설계(게이트) → plan.md+설계 문서
[완료] 2026-07-07 21:53:01 hp · #20 종료 — 완료조건 4건 전부 충족: ① ~/.namu 분리 모드 3층 실측 PASS ② 워커 배포=플러그인 동봉+엔진별 폴더 분리+호출명 폴백 규칙(스킬 5단계 반영, 설계 게이트 통과: 폴백=스킬 내장·yaml 부재 시 native 기본) ③ agy repo 밖 한계 목록 확정(-p+MCP 멈춤이 유일 실질 한계, 대화형 PASS) ④ plan.md 결정 테이블 4행+로드맵 해소 메모+대화 로그 요약 기록, docs/deploy_design.md 신설. 플러그인 0.1.3 범프. record 3건(01KWYA5BK2·01KWYA5XXJ·01KWYA6CPV, human). 커밋·푸시는 사용자 확인 대기
